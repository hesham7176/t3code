# Architecture

## الطبقات

- `data`: الوصول إلى الملفات والتخزين والتفضيلات.
- `domain`: نماذج مستقلة، فرز، أغلفة المجلدات، نتائج العمليات.
- `media`: محرك Media3 مستقل عن Compose.
- `ui`: شاشات Compose وViewModels.

## تدفق التصفح

`ExplorerViewModel` يطلب البيانات من `FileRepository` في `Dispatchers.IO`، ثم يعرض `StateFlow` غير قابل للتعديل. لا توجد عمليات I/O داخل Composable.

## أغلفة المجلدات

`FolderCoverResolver` يبحث محليًا بالترتيب `cover` ثم `poster` ثم `folder`، ثم يختار أول صورة مناسبة كبديل. لا يوجد فحص عودي عند فتح كل بطاقة.

## العمليات الخطرة

`FileOperationManager` ينفذ النسخ والنقل في الخلفية مع تعارضات آمنة وإشارات تقدم. الحذف ينقل العنصر إلى مجلد سلة المحذوفات مع حفظ المسار الأصلي.

## الوسائط

`MediaEngine` يغلف ExoPlayer ويحتفظ بحالة التشغيل وموضع الاستئناف. الواجهة تعرض `PlayerView` فقط، مما يسمح بإضافة خدمة تشغيل خلفي وMediaSession دون وضع منطق المحرك في Composables.

## حالة الميزات

| الميزة | الحالة |
|---|---|
| أساس Android وCompose وRTL | منفذة |
| الشاشة الرئيسية والتخزين | منفذة |
| تصفح الملفات والفرز وطرق العرض | منفذة |
| أغلفة المجلدات والـ thumbnails | منفذة أساسيًا |
| عمليات الملفات وسلة المحذوفات | منفذة أساسيًا |
| البحث والتحليل | منفذة أساسيًا |
| الصور والفيديو والصوت | منفذة أساسيًا |
| SAF وUSB OTG | البنية الأساسية منفذة، تعتمد الصلاحيات على الجهاز |
| ZIP | منفذ |
| الشبكة والسحابة | واجهات توسعة، لا يوجد مزود مفعّل افتراضيًا |
| محرر النصوص ومدير التطبيقات | منفذة أساسيًا |
| اختبارات وCI | منفذة أساسيًا، تحتاج تشغيلًا في بيئة Android |
